export enum RoleName {
  SUPER_ADMIN = "super_admin",
  TENANT_ADMIN = "tenant_admin",
  USER = "user",
}

export enum TwoFactorMethod {
  APP = "app",
  EMAIL = "email",
}

export enum RegistrationMode {
  SUPER_ADMIN_BOOTSTRAP = "bootstrap",
  TENANT_ADMIN_REGISTER = "tenant_register",
}